/**
 * Copy this file to `config.local.js` in the same folder (`src/`).
 * `config.local.js` is gitignored — put your Anthropic key there if you prefer not to use `.env`.
 */
module.exports = {
    ANTHROPIC_API_KEY: '',
};
